// Fill these in with your own Supabase project's values (Project Settings -> API).
// The anon key is safe to expose publicly -- it can only do what your Row Level
// Security policies allow (see supabase/schema.sql), which is "read/write your own rows".
export const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
